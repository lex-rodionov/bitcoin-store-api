const TRANSFER_TYPE = Object.freeze({
  WITHDRAW: 'withdraw',
  DEPOSIT: 'deposit',
});

const TRADE_TYPE = Object.freeze({
  BUY: 'buy',
  SELL: 'sell',
});

module.exports = {
  TRANSFER_TYPE,
  TRADE_TYPE,
}